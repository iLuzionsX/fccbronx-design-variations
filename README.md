# Fordham Community Church

A responsive, animated redesign for [fccbronx.org](https://www.fccbronx.org/) built with React and Vite.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The static site is generated in `dist/client` and deployed to GitHub Pages by the included workflow.
